"""Streamlit UI layer (Task 6).

A thin presentation layer over the Orchestrator + Report pipeline. Single
page, no chat history, no streaming — those are future work.

Run with::

    pip install -e ".[ui]"
    streamlit run src/formulation_os/ui/app.py
"""
