# 2026-07-03 — Session Log (resume + Ouyang platforms research)

## What was done

- **Verified project state**: `f53e7b6` is still HEAD on `main`, working tree clean, 18 commits total.
- **Researched Prof. 欧阳德方's platform portfolio** (the maturity bar):
  - `web_search` and `web_fetch` kept timing out — provider issue.
  - Workaround: direct `curl` to known endpoints. Crossref API (papers), GitHub API (repos), direct scrape of `*.computpharm.org`.
  - Mapped three live platforms: **FormulationAI**, **PharmSD**, **PharmDE** — all hosted under `*.computpharm.org`, all from his lab at University of Macau.
- **Wrote `docs/maturity/ouyang_platforms.md`** — portfolio, maturity characteristics (12-point bar), gap analysis (7 gaps), paper positioning, and 4 things to borrow immediately.
- **Pushed the analysis** to the repo.

## What was decided

- **The maturity bar is concrete now, not abstract.** Every Ouyang platform has: real models, curated data, multi-paper support, AD analysis, citation metadata, account system, experimental validation. That's the list to clear.
- **FormulationOS's positioning**: horizontal NL-orchestration layer above the vertical single-task platforms (Ouyang-style). Not competing — orchestrating.
- **Minimum paper bar**: wrap ≥1 Ouyang-style platform as a FormulationOS tool, validate on ≥1 published benchmark. Until then, the paper is a demo.
- **Search infra is flaky today.** Direct curl is the workaround. Future sessions: don't trust `web_search` timeouts as a "no results" answer; try curl on the known URL.

## What's next

- **Max to decide target venue** for the FormulationOS paper — ICML / NeurIPS / MLSys / ISCAS / other. Skipped last session, still open.
- **Paper skeleton (Abstract + Introduction)** once venue is known. Will pull from `docs/maturity/ouyang_platforms.md` for the related-work framing.
- **Task 8 (real model integration)** is back on the table as part of the maturity push. Spec from Max needed on which model to target first. My recommendation: wrap FormulationAI's cyclodextrin model as the first external tool — it's the most-cited work, the platform is live, and the input/output shape is well-documented.
- **Application-Domain pattern** — borrow from PharmSD, fits the LLM-planner's "how confident should we be?" framing.

## Open thread from prior session (still unaddressed)

- Paper venue. Two days now.
---

# 2026-07-03 — Afternoon continuation (AGENTS.md + doc alignment)

## What was done

- **AGENTS.md drafted (195 lines, project root)** — process manual for any coding agent (OpenClaw, Claude Code, Codex, etc.). Cross-references paper §3 and docs/maturity/. Includes 5-question reviewer checklist, end-of-milestone checklist, anti-patterns, project structure map, commit convention.
- **Positioning discussion with Max** — moved from "Scientific Operating System" → "AI-native workflow architecture instantiated in Computational Pharmaceutics". This is the framing Max wants for supervisor conversations and paper narrative.
- **Constitution discussion (not committed)** — Max proposed a 15-section Project Constitution. Reviewed and gave 5 pushbacks: §3 "avoid 'representation'" weakens reviewer defense; §4 walk-through needs illustrative-not-normative annotation; §8 directory list was 8 dirs but actual code has 11; §15 needs to specify "ask project lead"; the "additional principle" belongs in §5 not at end. No final version landed.
- **Committed the doc alignment batch** (`6134a55`, 10 files):
  - README.md, AGENTS.md (new), paper/abstracts/v1.md, paper/outline.md, paper/sections/{01,03,04,06,07,08}_*.md
  - "architecture frozen" → "initial prototype, abstractions designed for extensibility"
  - Added system flexibility statement to README and AGENTS.md §3
  - Fixed paper §8.3 LLM planner "not yet integrated" (it ships in v0.1)
  - Removed/softened stale "TODO. Write after Tasks X-Y land" markers in §1, §3, §4, §6, §7
  - Abstract: "complete system implementation" → "open-source prototype implementation"
- **Push from sandbox failed (TCP timeout to github.com:443)** — same intermittent issue as ba0c0b0 earlier. Max pushed from his Mac terminal successfully. Confirmed origin/main is now at 6134a55.

## What was decided

- **Workflow Abstraction is conceptual, not the DAG** — DAG is current representation. Future implementations may extend to branches/loops/agent iterations without breaking the abstraction. Codified in AGENTS.md §3, README callout, paper §3.
- **Literature tool rebrand (deferred, partial)** — concept renamed "Evidence Retrieval" in docs/tool descriptions; tool code name stays `Literature` for stability. Pushed back on full code rename (5+ files, no behavior benefit).
- **Domain discipline over "Generic Scientific OS"** — FormulationOS reads as a pharmaceutics platform first; broader claims appear only as motivation and future extensibility.
- **Architecture frozen vs abstractions flexible** — Layer structure frozen (5 layers, 11 directories); abstraction content flexible (Workflow may extend beyond DAG; Tool/Executor interfaces stable but evolving).
- **Push from sandbox is unreliable today** — known intermittent issue. Workaround: Max pushes from terminal, or use SSH remote (port 22).

## Pending items (carry-forward)

- **Project Constitution** — proposed, pushbacks given, no final version landed. Either finalize 15-section version or drop in favor of AGENTS.md as sole principle doc.
- **`docs/research_notes/` directory** — convention agreed, never created.
- **`docs/decisions/` directory with 4 retroactive ADRs** — proposed but not written: 0001-scientific-workflow-abstraction, 0002-sts-as-extension-schema, 0003-rule-based-planner-as-default, 0004-mock-tools-as-v0.1-substrate.
- **README restructure as "Project Proposal + Technical Overview"** — proposed but not done.
- **Real model integration (Task 8)** — first target: HTTP executor against `formulationai.computpharm.org`.
- **Validation suite** — paper §7 numbers (no eval data yet).

## Session log pointers

- Morning session (this file, upper): bootstrap + Ouyang platforms research
- 6134a55: doc alignment batch (afternoon)
- ba0c0b0: maturity analysis
- f53e7b6: bootstrap + first memory file
