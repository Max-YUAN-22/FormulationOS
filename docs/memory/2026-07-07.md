# 2026-07-07 — Session Log (Phase 2 strategy reset)

## What was done

- **Resumed from cold.** Read `docs/memory/2026-07-03.md` to recover
  context: v0.1 MVP on `main`, 159 tests passing, 5 mock tools live,
  paper §1–§8 drafted, Ouyang portfolio mapped. No commits since
  `4ac26ee docs: remove internal meeting prep from public repo`.

- **Inspected dirty working tree (8 uncommitted files).** All targeted
  at upgrading `formulation_dt/` from mock to "real Wang 2024 adapter".
  Found three critical anti-patterns:
  1. `adapter.py` implemented 12 `_decision_*` methods using Lipinski
     Ro5 + MW + logP + BCS approximation — **fabricating predictions
     in place of the real FormulationDT model** (violates AGENTS.md
     §12 mock-as-production).
  2. `core/tool.py` added a `Citation` Pydantic field to `ToolSpec`
     outside STS v0.2 (spec is `extra="forbid"`; v0.3 not yet
     scoped).
  3. `pyproject.toml` added `[formulation_dt]` extra pointing at
     `git+https://github.com/NamanWang/FormulationDT.git` — but
     upstream has no packaged inference API or pretrained weights,
     so the extra installs something that does not run a model.
  4. Tests asserted `len(formulation_decisions) == 12` — pinning the
     fabricated-prediction behaviour into the test suite.

- **Discarded dirty tree.** `git checkout -- . && git clean -fd`.
  HEAD back to clean `4ac26ee`. 151 tests still passing.

- **Phase clarification from Max.** Confirmed:
  - **Phase 2.1 = adapter pathway** (STS can wrap a real tool
    interface; no model integration claim).
  - **Phase 2.2 = real model integration** (requires upstream
    cooperation; deferred).
  - **Never fabricate predictions** — core principle.
  - **STS = Scientific Semantics Extension** (4 sub-extensions:
    Scientific Semantics, Scientific Dependencies, Planning Hints,
    Provenance Specification). Currently docs use "extension schema"
    / "Scientific Tool Specification (STS)" — naming drift to fix
    later.
  - **Reproducibility = reproducible execution trace**, not
    bit-identical. Already correctly stated in docs.
  - **New tool path:** `src/formulation_os/tools/builtins/formulation_dt_real/`
    (sibling to existing `formulation_dt/` mock, not replacement).

- **Probed NamanWang/FormulationDT (GitHub API).** Findings:
  - Repo exists, MIT, 1.6 MB, Jupyter Notebook primary language,
    0 stars, last updated 2025-08-09.
  - README 180 bytes — minimal.
  - Top-level: `Code/`, `Data/`, `TrainingRecords_MLP/`, `LICENSE`.
  - **No `setup.py` / `pyproject.toml`** — not pip-installable.
  - **No large model files (`.pkl`, `.h5`, `.pt`, `.joblib`) in
    tracked history** — training code only, no pretrained weights.
  - README claims deployment at `http://formulationdt.computpharm.org/`
    but the URL **serves a 2023 HTTrack mirror of
    `formulationai.computpharm.org`** (HTML body authored by Jie
    Dong, describes FormulationAI's drug solubilization — not
    FormulationDT). `/api`, `/predict`, `/swagger`, `/docs` paths
    all 404. **The FormulationDT web service is not actually
    deployed at this URL** (or is at a different URL behind auth).

- **Wrote 3 research-note files** (markdown only, no code, no commit):
  1. `docs/research_notes/README.md` — index + AGENTS.md §5 reminder.
  2. `docs/research_notes/formulation_dt_adapter.md` — 5-question
     reviewer checklist (Contribution / Evaluation / Figure /
     Criticism / Citation) + Appendix A (repo investigation) +
     Appendix B (questions for 师兄).
  3. `docs/research_notes/scientific_model_integration_matrix.md` —
     5-platform integration matrix + 3 integration strategies
     (HTTP / Python Adapter / Docker) + Phase 2 sequencing + decision
     tree + open questions.

## What was decided

- **Discard, not repair.** The dirty tree's direction was wrong in
  four independent ways (path, fabricated predictions, STS
  violation, misleading extras). Repairing would have been more
  expensive than starting from the documented pattern.
- **Phase 2.1 ships only the adapter pathway.** No fabricated
  predictions; no `Citation` field outside STS; no `[formulation_dt]`
  pip extra; no `len == 12` test. Adapter returns `model_unavailable`
  with explicit status.
- **Adapter goes in a new `formulation_dt_real/` directory**, sibling
  to the existing mock. Existing `formulation_dt/` (dissolution
  profile mock) is untouched on `main`.
- **Web probe finding is reportable.** `formulationdt.computpharm.org`
  is unreliable — bring this to 师兄 as evidence that the deployed
  service path needs verification, not assumption.
- **FormulationAI / PreformulationAI / AI-PBPK are the higher-priority
  Phase 2.3+ targets** once adapter pathway ships, since they have
  the most citations and most mature web deployments.

## What's next (this session, on Max's terms)

- **Max to take the integration matrix + FormulationDT research note
  to 师兄.** Open questions are listed in
  `docs/research_notes/scientific_model_integration_matrix.md` and
  `docs/research_notes/formulation_dt_adapter.md` Appendix B.
- **Phase 2.1 implementation** (the adapter) is blocked on the lab
  discussion outcome. If lab says "yes, pretrained weights + API
  coming", then adapter code is the next commit. If lab says "no,
  use a different platform first", then FormulationAI / PreformulationAI
  HTTP-executor investigation becomes Phase 2.1.
- **No code committed this session.** Three markdown files written
  but not committed. Max to decide commit granularity:
  - (a) single `docs(research): add Phase 2 integration research
    notes` commit (cleanest), or
  - (b) three separate commits per file (more granular), or
  - (c) defer commit until after 师兄 discussion (keeps options open).

## Pending items (carry-forward)

- **Research notes files written but not committed.** See "What's
  next" above.
- **STS naming drift.** Docs currently call STS "Scientific Tool
  Specification"; Max's latest framing is "Scientific Semantics
  Extension". Both names should be reconciled in v0.2.x (the four
  sub-extension names are correct: Scientific Semantics, Scientific
  Dependencies, Planning Hints, Provenance Specification). Defer
  until after 师兄 discussion to avoid premature docs churn.
- **`docs/capability_taxonomy.md`** — Max explicitly said "不要叫
  Capability Taxonomy". The file currently frames itself as
  "Scientific Semantics — Current Capabilities", which is closer to
  the right framing. Either rename to `docs/scientific_semantics.md`
  or merge into STS spec §3. Defer.
- **`docs/system_design.md` §6.1 Option B** — the existing
  FormulationDT adapter example is aspirational (assumes upstream
  has `from formulation_dt import predict_decision_1`). Should be
  updated to reflect the adapter-with-`model_unavailable` pattern.
  Defer until adapter code lands.
- **Provenance record format.** Paper §6.3 shows the full
  `execution_id / tool_version / input_hash / output_hash / ...`
  schema, but v0.1 Orchestrator only captures `duration_ms` and
  `warnings`. Real provenance emission is a follow-up.
- **Real model integration (Phase 2.2).** Blocked on lab.
- **Validation suite (paper §7 numbers).** Blocked on real data.

## Session log pointers

- This file (`docs/memory/2026-07-07.md`) — Phase 2 strategy reset
  + research notes.
- `docs/research_notes/` — three new files (index + FormulationDT
  + matrix).
- `4ac26ee` — HEAD on `main`, working tree clean, 151 tests passing.