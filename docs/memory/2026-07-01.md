# 2026-07-01 — Session Log (catch-up + push + bootstrap)

## What was done

- **Verified project state on disk** — `~/FormulationOS` exists, clean working tree, 17 commits on `main` at `edc1896` ("docs: add Figure 1 (Mermaid architecture diagram) and polish README").
- **Pushed the local history to GitHub** — origin was already configured to `https://github.com/Max-YUAN-22/FormulationOS.git`. First attempt hung on credentials (`gh auth setup-git` was applied; second run with `GIT_TERMINAL_PROMPT=0` and a longer timeout succeeded — `* [new branch] main -> main`).
- **Confirmed test suite** — `pytest` reports `151 passed in 1.55s`. (The transcript's claim of 151 was accurate.)
- **Network check** — `curl api.github.com` returns `HTTP/2 200`; `nc github.com 443` succeeds. The previous session's "sandbox can't reach GitHub" was stale; the sandbox is currently online.
- **Bootstrap (this session)**:
  - Named myself: **Mavis** 🪄
  - Filled in `IDENTITY.md` and `USER.md`
  - Updated `SOUL.md` with the FormulationOS mission + memory protocol
  - Wired up `HEARTBEAT.md` with periodic checks for the repo
  - This memory file is the first entry in the new convention.

## What was decided

- **Name**: Mavis. Reason: literary name, songbird, deep-cut Trillian reference; fits a research AI that turns NL into real computations. Emoji 🪄.
- **Memory convention**: write session logs to `~/FormulationOS/docs/memory/YYYY-MM-DD.md` after every session, with three sections — What was done / What was decided / What's next. Committed to the repo so the project carries its own history.
- **Bootstrap is required within 5 min of first contact** with a new project. No more identityless sessions drifting into multi-hour implementation.
- **The previous transcript-only recap pattern is over.** Future sessions catch up by reading the latest memory file, not by Max pasting context.
- **Project north star shifted**: from "ship paper" to "build a truly mature platform" that holds up against Prof. 欧阳德方 (Ouyang Defang)'s platforms (DeepFormulation et al.). Paper skeleton is still on the list, but it's a deliverable, not the goal.

## What's next

- **Decide target venue** for FormulationOS paper — ICML / NeurIPS / MLSys / ISCAS / other. Max to confirm.
- **Study Prof. Ouyang Defang's platforms** — DeepFormulation, related work. Need to understand the bar for "mature" before claiming any feature is ready. This session: ran initial web searches, more research to come.
- **Draft Abstract + Introduction framework** once venue is known.
- **Task 8 (real model integration)** — originally deferred, now back on the table as part of the maturity push. Will need a real spec from Max on which models to target.
- **Continue deepening v0.1** — caching, provenance, DAG runner (the original Task 8-options menu). Pick deliberately, not reflexively.

## Open threads from the transcript that need follow-up

- The plan-A-is-broken moment: confirmed the test fix landed (`a4650d4` — "exercise literature_review keyword in literature test"), but the substantive question of how the planner handles "papers on X" vs "literature review on X" is still open. Tracked under "planner scoring" in the project. Worth a follow-up design conversation when there's time.
